# 任务记录 2026-07-10-001

## 目标
- 将小程序运行时配置文件统一为 miniprogram/config.js，不再使用 config.local 或 config.local.js。

## 方案
- 新增 miniprogram/config.js 并填入运行时配置。
- 修改 miniprogram/app.js 配置加载路径与错误提示。
- 更新 .gitignore 忽略项与文档中的配置文件说明。

## 进度
- [x] 现状排查完成
- [x] 任务文件创建
- [x] 代码与文档修改
- [x] 修改后校验

## 验证结果
- `node -e "require('./miniprogram/config.js')"` 通过，配置可加载。
- `miniprogram/**` 下未再检索到 `config.local` / `config.local.js` 引用。
- `miniprogram/app.js`、`miniprogram/README.md`、`docs/01-specs/01_06_Miniprogram_Config_Guide.md`、`.gitignore` 无语法/诊断错误。

## 风险
- 若仍保留旧文件可能造成认知混淆，但运行时将只读取 config.js。
