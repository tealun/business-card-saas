| ID | Trigger/Evidence | Root Cause | Local Fix | Systemic Action | Guard Verification | Closure Level | Next Planning Input |
|---|---|---|---|---|---|---|---|
| EV-2026-07-15-demo-asset-cache | Confirmed: after replacing demo company images, WeChat DevTools still rendered old placeholder-style assets for reused filenames while newly named assets rendered correctly. Evidence: user screenshots after commit `d7256b3`, followed by cache-busting fix `a5d15be`. | Runtime/operations gap: demo assets were served with `cache-control: public, max-age=31536000, immutable`, but demo fixture and Mini Program fallback reused stable media URLs when asset bytes changed. | Added `?v=20260715-photo2` to backend demo fixture URLs and Mini Program demo fallback URLs. | Regression guard: public-card controller test now collects all demo asset URLs in the demo public-card response and requires every one to include `?v=`. | `npm test -- public-card.controller.spec.ts` must fail if a backend demo asset URL omits a version parameter. | Guarded | Future demo media refreshes must either use new filenames or update the version parameter in both backend fixture and Mini Program local fallback. |
| EV-2026-07-15-production-env-drift | Confirmed: after deploying new required WeCom settings, production startup failed because the protected server `.env` lacked `WECOM_PROVIDER_CORP_ID`; running the database package script from `database/` also failed with `DATABASE_URL is required` because the root `.env` was not loaded. Evidence: production startup and migration outputs supplied during the 2026-07-15 WeCom rollout. | Runtime/operations gap: deployment synced the updated `.env.example` but correctly preserved the existing `.env`; the runbook neither required key-name reconciliation nor used an explicit root env file for database commands. | The missing provider setting was added to the server `.env`, after which the WeCom callbacks could be validated; the failed migration invocation was corrected to explicitly load the root env file. | Deployment gate: the authoritative backend guide now requires a no-secret key-name reconciliation after sync and mandates root-level `node --env-file=.env database/scripts/...` commands for migration/readiness. | Guide validation must find `WECOM_PROVIDER_CORP_ID`, `WECOM_SENSITIVE_REDIRECT_URI`, the `comm -23` reconciliation gate, and both explicit `--env-file=.env` database commands; the deployment checklist must contain no plain database-directory `npm run migrate` instruction. | Institutionalized | Every future `.env.example` change is a deployment checklist event; reconcile key names before migration/build/restart and never overwrite the production `.env`. |
| EV-2026-07-17-admin-auth-plan-landing | Confirmed: the admin account auth planning package was drafted outside the repo with assumed numbering (`migrate_v1_13`); at landing time the repo already contained an unrelated `migrate_v1_13.sql` (99_71 admin_operation_logs hardening) — a numbering collision. Evidence: glob of `database/migrations/` at landing (2026-07-17). | Process gap: planning artifacts drafted outside the repo assigned sequence numbers at draft time without re-verifying namespace occupancy at landing time. | Renumbered the migration to `migrate_v1_14.sql`; re-scanned all four namespaces and confirmed `01_09` / `02_04` / `99_72` were still free before landing `01_09_Admin_Account_Auth_System_Spec.md`, `02_04_Admin_Account_Auth_Gates.md`, `99_72_admin_account_auth_readiness.md`. | Landing rule: any planning artifact drafted outside this repo must re-glob target namespaces immediately before landing; migration numbers are assigned at landing time, never at draft time. | Before landing, glob shows no same-numbered target file; after landing, `docs/README.md` index rows resolve to real files. | Guarded | Execute 02_04 M0 evidence gates (WeCom `3rd_qrConnect` / `getuserinfo3rd` / `get_admin_list`) before any scan-login coding; platform account CRUD (M1-S4) may start in parallel. |
| EV-2026-07-18-wecom-login-suite-boundary | Confirmed: WeCom service-provider login authorization exposed its own SuiteID/Secret and Ticket, while earlier fixes used the ordinary third-party app SuiteID/Secret for `ServiceApp` login; after scan callback, the admin page could return to the login screen without preserving the backend failure. Evidence: commits `d032ff8`, `4943a00`, and `bebf4e7`; user screenshots showed `appid` errors, then successful scan return without session entry. | Design/contract gap plus test gap: the project treated service login authorization as the same suite boundary as the ordinary third-party app and lacked executable coverage for callback parameter aliases and visible scan-login failure handling. | Split `WECOM_LOGIN_SUITE_ID/SECRET` and login callback secrets from ordinary app credentials; login callback stores its own `suite_ticket`; scan login uses login suite token for `getuserinfo3rd` and ordinary suite token for `get_admin_list`; frontend/backend accept `code` or `auth_code` and retain callback errors. | Regression guards: admin scan-auth tests assert login SuiteID/token usage; login-callback tests assert login-suite ticket storage; suite-token tests assert separate login suite token refresh; admin-auth contract test accepts `auth_code`; admin static/JS syntax checks cover the callback-error UI path. | `npm.cmd test -- admin-auth admin-wecom-scan-auth`, `npm.cmd test -- admin-wecom-scan-auth wecom-login-callback wecom-suite-token`, `npm.cmd run typecheck`, `npm.cmd run lint`, `npm.cmd run build`, `node scripts/verify-admin-static.mjs`, JS syntax check, and `git diff --check` passed before ledger update. | Institutionalized | Future WeCom work must classify console pages by credential boundary first: ordinary third-party app, login authorization, data callback, sensitive-info authorization, or mini-program association; each boundary needs separate env names, callback token/AESKey, ticket source, and tests before production validation. |
