| ID | Trigger/Evidence | Root Cause | Local Fix | Systemic Action | Guard Verification | Closure Level | Next Planning Input |
|---|---|---|---|---|---|---|---|
| EV-2026-07-15-demo-asset-cache | Confirmed: after replacing demo company images, WeChat DevTools still rendered old placeholder-style assets for reused filenames while newly named assets rendered correctly. Evidence: user screenshots after commit `d7256b3`, followed by cache-busting fix `a5d15be`. | Runtime/operations gap: demo assets were served with `cache-control: public, max-age=31536000, immutable`, but demo fixture and Mini Program fallback reused stable media URLs when asset bytes changed. | Added `?v=20260715-photo2` to backend demo fixture URLs and Mini Program demo fallback URLs. | Regression guard: public-card controller test now collects all demo asset URLs in the demo public-card response and requires every one to include `?v=`. | `npm test -- public-card.controller.spec.ts` must fail if a backend demo asset URL omits a version parameter. | Guarded | Future demo media refreshes must either use new filenames or update the version parameter in both backend fixture and Mini Program local fallback. |
