# 99_71 — Admin Console Rebuild & Operation Audit Log — Deep Audit — 2026-07-17

## Scope
- Range: `f203a95..b5246ad` (9 commits, 54 files, +6224/-1223)
- Commits: 2bb8d64 (console redesign) · 0e3874b (login redesign) · b261d0c (platform console rebuild) · 22814bb (design-gap eval doc) · 140fadb (member fields + admin status + tenant auth guard) · 7adeb82 (tenant console rebuild) · d93091f (operation log + instrumentation) · a60a82b (time-window filter) · b5246ad (wire operation log into UI)
- Baseline: branch `main`@`b5246adb7ef439519ef9642a847eee03ede40b0c`; worktree clean; no uncommitted changes
- Auto-selected depth: **Deep** (score 9: admin surface +3, auth/session +3, PII fields +3; multi-tenant SaaS with payment/commercial plans)
- Prior audit baseline: `99_70_admin_design_gap_evaluation.md` (developer's own intent note for this range — treated as a claim to verify, not fact)

## System Goal & Critical Paths
Protected properties: (1) tenant/platform admin surfaces stay isolated by `accountType`, (2) tenant data (member PII, cards) stays isolated by `tenant_id`, (3) the new operation-audit-log is a trustworthy compliance record.

- **Path: platform-token vs tenant-token isolation across 6 admin controllers** — Healthy. `requireTenantAdminRole` correctly checks `accountType === "tenant"` and is applied in the service layer of all 6 claimed modules; verified by direct code read, not just agent claim.
- **Path: admin write → operation log → audit read** — **At risk**. Write path is correctly tenant-scoped and PII-scrubbed, but (a) the underlying table has no RLS/FK, breaking this project's own tenant-isolation convention and **currently failing the CI deploy gate**, and (b) the `ip` field recorded is trivially spoofable by any caller. See findings below.
- **Path: `PATCH /admin/admins/:adminId` (owner-only status toggle)** — Healthy in implementation (self-modify block, owner-row block, tenant-scoped SQL WHERE), but **Verification gap**: zero repository-level test exists for this endpoint, so a future regression dropping the `tenant_id` clause would go undetected.
- **Path: admin card field merge (data-loss bug fix)** — Healthy. Overlay merge is correct and injection-safe (strict zod schema, no passthrough).

## Confirmed Strengths
- `requireTenantAdminRole` isolation guard: correctly implemented and consistently applied across admin-management, admin-config, admin-analytics, admin-commercial, admin-observability, company-video-feature.
- `/admin/commercial` correctly excludes `operator` role via an explicit allow-list, not just a rank threshold (a rank-threshold check alone would NOT have excluded operator, since auditor is the lowest rank).
- Operation-log write path is fire-and-forget-safe (never blocks/rolls back the primary business write) and does not log PII/secrets into `detail_json` (verified across all ~15-18 instrumentation call sites).
- Tenant scoping on `GET /admin/operation-logs` and cross-tenant IDOR protection on the admin-status endpoint are both correctly implemented at the SQL layer (not app-code-only).
- Admin frontend (`admin/app.js`, 2768 lines, no build step): systematic `escapeHtml()` use at every server-data render site — no XSS found; mobile/email masking is real (masked value is the only thing written to the DOM, not a cosmetic overlay); admin token lives in `sessionStorage`, never logged.
- Full test suite green: 56 suites / 288 tests, 0 skipped, 0 failed. No debug residue, no dead functions, no `.skip`/`.only` in tests, no new unpinned dependencies, consistent naming conventions across all new files.
- `deploy-backend.yml` genuinely gates deploy on audit/typecheck/lint/test/build with no `continue-on-error` — real enforcement, not advisory (which is what makes the RLS-gate break below consequential rather than cosmetic).

## Verification Gaps
- No integration/e2e layer exists anywhere in the repo (only unit tests with mocked DB/session) — cross-controller enforcement of the new auth guard is unverified end-to-end.
- `migrate_v1_12.sql` has never been applied against a live Postgres instance in CI; only mocked-DB unit tests and the (currently failing) static `validate-rls.cjs` regex check exercise it.
- No repository-level test for `admin-observability.repository.ts`'s tenant-scoped UPDATE — the IDOR protection is correct today but unguarded against regression.

## Findings

### P1 — Should Fix Soon
| ID | Type | Confidence | Status | Title | Path/Dimension | File | Line | Evidence | Fix |
|----|------|------------|--------|-------|----------------|------|------|----------|-----|
| A71-P1-1 | Confirmed | High | Fixed | `admin_operation_logs` breaks project's mandatory RLS convention; `npm run rls:validate` currently fails, blocking every backend deploy since d93091f | Deploy/Ops · Info Isolation (dims 6/12) | database/migrations/migrate_v1_12.sql:1-18; database/schema.sql:491-507; database/scripts/validate-rls.cjs:10-17 | Reproduced: `node database/scripts/validate-rls.cjs` → `Error: admin_operation_logs must enable RLS`, exit 1. This step runs unconditionally in `.github/workflows/deploy-backend.yml:77` after tests/typecheck/build pass. Table has `tenant_id BIGINT NOT NULL` (triggers the validator's tenant-table detection) and no FK to `tenants(id)`, unlike every other tenant-scoped table added since v1_9 (`rls.sql` has 20 `ENABLE ROW LEVEL SECURITY` statements covering comparable tables). App-layer code correctly filters by `tenant_id` today, so there is no confirmed cross-tenant leak yet — but RLS is this project's documented defense-in-depth layer, and it's absent here with zero acknowledgment (not added to `tenantRlsExceptions`, not intentionally excepted with a comment as the one other exception in that list does). Note: an earlier claim (from one review pass) that "no RLS exists anywhere in this project" is **factually wrong** and was corrected during cross-verification — RLS is the norm, this table is the outlier. | Either (a) add FK + RLS policy for `admin_operation_logs` mirroring `migrate_v1_10.sql`/`migrate_v1_11.sql`, using a platform-bypass policy clause if cross-tenant platform reads are required, or (b) if no-RLS is a deliberate, reviewed exception, add it to `tenantRlsExceptions` in `validate-rls.cjs` with the same kind of justification comment already used for `wecom_sensitive_auth_states`. Either way, unblock CI now. |
| A71-P1-2 | Confirmed | High | Fixed | Audit-log `ip` field is spoofable via client-controlled `X-Forwarded-For`, undermining the compliance record | Security · Deploy/Ops (dim 3) | backend/src/admin-auth/admin-auth.guard.ts:31-37 | `resolveClientIp()` takes the **first** (client-supplied, leftmost) CSV entry of the raw `x-forwarded-for` header unconditionally, instead of Fastify's own trust-boundary-aware `request.ip` (app configured with `trustProxy: "loopback"` in backend/src/main.ts:23). Any external caller can prepend an arbitrary value. The test at admin-auth.guard.spec.ts:32-46 codifies this as expected behavior (`"203.0.113.9, 10.0.0.1"` → `requestIp === "203.0.113.9"`, the attacker-controlled value). This directly undermines the stated purpose of the operation log (合规与安全追溯). | Use `request.ip` (already resolves correctly per `trustProxy: "loopback"`) instead of manually parsing `x-forwarded-for`, or if a proxy chain genuinely requires header parsing, take the right-most hop appended by the trusted proxy, not the left-most client-supplied one. |
| A71-P1-3 | Confirmed | Medium | Fixed | RBAC platform/tenant isolation guard has no integration coverage; 2 of 7 guarded modules have zero unit-level rejection test | Testing (dim 11) | backend/src/admin-config/admin-config.service.spec.ts (no test); backend/src/company-video-feature/company-video-feature.controller.ts:16 (no controller spec) | `admin-config.service.ts` has 7+ call sites of `requireTenantAdminRole` with no test asserting a platform-scoped session is rejected (confirmed via content grep of the spec file). `company-video-feature.controller.ts` calls the guard directly with no corresponding spec at all. No test anywhere drives a request through `AdminAuthGuard` + a real signed token + actual routing, so cross-controller enforcement is unverified end-to-end even for the 5 modules that do have unit-level checks. | Add a platform-token-rejection test for admin-config and company-video-feature matching the pattern already used in admin-management/admin-observability specs; consider one thin e2e test that drives a real HTTP request through the full guard stack for at least one tenant and one platform route. |
| A71-P1-4 | Verification gap | Medium | Fixed | Cross-tenant IDOR protection on `PATCH /admin/admins/:adminId` is correctly implemented but has zero repository-level test | Testing (dim 11) | backend/src/admin-observability/admin-observability.repository.ts:~99-150 (no admin-observability.repository.spec.ts exists) | `getTenantAdmin`/`updateTenantAdminStatus` correctly scope by `WHERE tenant_id = $1 AND id = $2`, but the service spec mocks the repository entirely — a future regression dropping the `tenant_id` clause (reintroducing cross-tenant IDOR on a privilege-affecting endpoint) would not be caught by any test today. | Add `admin-observability.repository.spec.ts` asserting the UPDATE/SELECT WHERE clauses include tenant scoping, following the pattern of other repository specs in this codebase. |
| A71-P1-5 | Confirmed | Medium | Fixed | Audit-log write failures are silently swallowed with zero observability | Deploy/Ops (dim 12) | backend/src/admin-operation-log/admin-operation-log.service.ts:31-53 | `record()` wraps the insert in `try { … } catch { /* intentionally ignored */ }` with no logger call and no metric. Combined with A71-P1-1 (the write path would break on a fresh DB that correctly enables RLS without a matching app-side `SET app.tenant_id`), a total failure of the compliance log would be invisible to monitoring — no health check probes this path. | Log the swallowed error at `warn` level (still without blocking the caller) so failures are at least observable, and/or add a lightweight counter metric. |
| A71-P1-6 | Confirmed | Medium | Fixed | TOCTOU on tenant-admin self/owner-row guard: check-then-act instead of atomic SQL re-assertion | Data Accuracy (dim 7) | backend/src/admin-observability/admin-observability.service.ts:34-49 | `updateTenantAdminStatus` fetches the target row, checks self-modify/owner-role in application code, then issues a separate UPDATE whose WHERE clause re-scopes by `tenant_id`/`id` but not by `role <> 'owner'` or `open_userid <> session.openUserid`. Two concurrent requests (e.g., a role promotion racing a disable) could slip through the check-then-act window. The platform sibling (`updatePlatformAdminStatus`, same file) does the self-check atomically in SQL (`WHERE id = $1 AND username <> $3`) — a stricter pattern already proven in this codebase. | Mirror the platform path: fold the self/owner-row conditions into the UPDATE's WHERE clause so the guard is atomic. |

### P2 — Nice to Have
| ID | Type | Confidence | Status | Title | Path/Dimension | File | Line | Evidence | Fix |
|----|------|------------|--------|-------|----------------|------|------|----------|-----|
| A71-P2-1 | Confirmed | High | Deferred | Raw mobile/email returned by API to `auditor` role despite UI-only masking | Info Isolation (dim 6) | backend/src/admin-management/admin-management.repository.ts:178-179; admin-management.service.ts:48; admin/app.js:599-600 | `GET /admin/members` returns unmasked mobile/email to any tenant role ≥ `auditor`; masking (`maskMobile`/`maskEmail`) is applied only at render time in the frontend. Direct API access (devtools/curl with a valid auditor token) bypasses the mask. Same-tenant data, not a tenant-boundary break — a UX/design gap, not an authz break. | If auditor is meant to see masked-only, mask server-side and gate raw values behind admin/owner; otherwise document the masking as UX convenience, not a control. |
| A71-P2-2 | Likely | Medium | Fixed | pino redact list uses flat paths that don't match nested `fields.*` card payload shape | Security (dim 3) | backend/src/app.module.ts:39-54; backend/src/contracts/employee-card.ts:67-80 | Redact config lists `req.body.mobile/phone/email/wechat_id`, but card payloads nest these under `fields.*`. No confirmed sink currently logs `req.body` (pino-http's default req serializer excludes body), so no active leak — but the redact config is defensively wrong and would misfire the moment any interceptor/serializer starts logging bodies. Pre-existing pattern, not newly introduced by this range, but touches the same PII surface this audit is reviewing. | Add `req.body.fields.mobile/.phone/.email/.wechat_id` to the redact paths, or use a wildcard pattern. |
| A71-P2-3 | Confirmed | High | Fixed | Admin-status endpoints lack an explicit `@Throttle`, unlike sibling write endpoints | Security (dim 3) | backend/src/admin-observability/admin-observability.controller.ts:22,33 | Still covered by the global `ThrottlerGuard` (300 req/min), just looser than comparable writes elsewhere in admin-config/admin-management. Low impact. | Add a route-level `@Throttle` consistent with sibling privilege-affecting endpoints. |
| A71-P2-4 | Confirmed | High | Fixed | `updatePlatformAdminStatus` response bypasses zod validation that its own schema already defines | Coding Standards (dim 10) | backend/src/admin-observability/admin-observability.service.ts:69-86 | Returns the repository object directly with no `.parse()`, unlike the sibling `updateTenantAdminStatus` (line 53) which validates via `tenantAdminSummarySchema.parse(updated)`. `platformAdminSummarySchema` already exists and is used elsewhere in the same file. Currently harmless (shapes match) but silently defeats the "every response validates" convention. | One-line fix: `return platformAdminSummarySchema.parse(updated);` |
| A71-P2-5 | Verification gap | Low | Fixed | No regression test for "admin edit preserves employee self-service fields" (the exact scenario the merge-fix was for) | Testing (dim 11) | backend/src/admin-management/admin-management.repository.spec.ts:113 | The existing spec seeds the pre-edit blob without `department/company/website`, so it never exercises the described data-loss scenario. The fix itself (`mergeFields`/`normalizeFields`, repository.ts:687-721) is correct by inspection. | Add a test case seeding the blob with employee-set fields, then asserting an admin partial-update preserves them. |
| A71-P2-6 | Confirmed | Medium | Fixed | No inline error state on refresh across new admin pages — stale data can look current | UX (dim 9) | admin/app.js:171-198, 340-361 | On fetch failure after initial render, `run()` only shows a 3.2s auto-dismissing toast; the table itself is never re-rendered with an error state, so a missed toast leaves stale data with no visible indicator. Applies uniformly to member list, both operation-log pages, analytics, admin/account status pages. Pre-existing pattern extended to new pages, not a new regression. | On catch, render an inline error banner/row in the affected table region in addition to the toast. |
| A71-P2-7 | Confirmed | Low | Fixed | No active loading indicator during fetch/refresh | UX (dim 9) | admin/index.html:263,561,588,826,869 | Initial state is a static "尚未加载" placeholder; no spinner/skeleton while a load/refresh is in flight. Minor. | Add a lightweight in-flight indicator if refresh latency becomes noticeable. |
| A71-P2-8 | Confirmed | Medium | Deferred | Duplicated `escapeLike`/`iso`/pagination-filter boilerplate perpetuated into the new operation-log module instead of consolidated | Code Efficiency (dim 4) | backend/src/admin-management/admin-management.repository.ts:669; admin-observability.repository.ts:388,392; admin-operation-log.repository.ts:183,187 | Identical helper functions and near-identical `{whereSql, values}` + `count(*) OVER()` pagination pattern duplicated across 3 repositories. Pre-existing anti-pattern; the new module was a missed opportunity to extract a shared helper rather than a new instance of the problem. | Extract to a shared `database/query-helpers.ts` once convenient; not urgent at 3 occurrences. |
| A71-P2-9 | Likely | Medium | Fixed | No index serves unfiltered platform-wide operation-log queries (common case for a platform auditor) | Code Efficiency (dim 4) | backend/src/admin-operation-log/admin-operation-log.repository.ts:139-163; database/migrations/migrate_v1_12.sql:18 | `listPlatformLogs` without a `tenant_id` filter sorts `ORDER BY created_at DESC` over the whole table; the only index has `tenant_id` as the leading column and can't serve this scan/sort. Low risk at current audit-log volume. | Add a plain `(created_at DESC)` index if platform-wide browsing becomes a hot path. |
| A71-P2-10 | Confirmed | High | Fixed | Two unused exported types (dead code) | Coding Standards (dim 10) | backend/src/contracts/admin-observability.ts:92-93 | `UpdatePlatformAdminStatusRequest`/`UpdateTenantAdminStatusRequest` exported but never imported anywhere; the underlying schemas ARE used correctly via `.parse()`. Harmless noise. | Remove the two unused type aliases. |

## Fix Guide
1. **A71-P1-1** — unblock CI first: decide RLS-vs-documented-exception for `admin_operation_logs`, then fix `validate-rls.cjs` or add the migration. This is currently release-blocking.
2. **A71-P1-2** — swap `resolveClientIp` to use `request.ip`; update the guard spec accordingly.
3. **A71-P1-6** — fold self/owner-row checks into the tenant-admin-status UPDATE's WHERE clause, mirroring the platform path already in the same file.
4. **A71-P1-5** — log (don't just swallow) audit-log write failures.
5. **A71-P1-3 / A71-P1-4** — add the missing rejection/IDOR tests; low effort, closes real verification gaps on privilege-affecting endpoints.
6. P2 items — batch into routine cleanup; none block anything.

## Fix Verification — 2026-07-17

All P1s and 8 of 10 P2s fixed; changes are uncommitted in the working tree pending user review/commit (no commit hash yet — this section will need its verification hash added once committed).

- **A71-P1-1**: resolved via whitelist, not the originally-planned RLS refactor. During implementation, found `database/rls.sql` already has a directly analogous precedent — `callback_events`, a platform-ops table with the same cross-tenant-read need — deliberately excluded from RLS with a justification comment. Confirmed with the user (who initially asked for a full RLS refactor) that the whitelist approach matches established project convention better; a true RLS refactor would additionally have required routing `admin-operation-log.repository.ts` through `TenantTx` (it currently queries the pool directly, matching `callback_events`, not the RLS-protected tables) and inventing a new platform-bypass primitive with no existing precedent. Implemented: added `admin_operation_logs` to `tenantRlsExceptions` in `database/scripts/validate-rls.cjs` with a justification comment; mirrored `callback_events`' `DISABLE ROW LEVEL SECURITY` documentation block in `database/rls.sql`; added the previously-missing `tenant_id` FK and a `(created_at DESC)` index (closing A71-P2-9 too) via new migration `database/migrations/migrate_v1_13.sql`, with `database/schema.sql` updated to match. Verified: `node database/scripts/validate-rls.cjs` now exits 0.
- **A71-P1-2**: `resolveClientIp()` now uses Fastify's own `request.ip` (already trust-boundary-aware per `trustProxy: "loopback"`) instead of manually parsing `X-Forwarded-For`. Spec updated to assert the header is no longer trusted directly.
- **A71-P1-6**: `updateTenantAdminStatus`'s SQL UPDATE now atomically re-asserts `role <> 'owner'` and `open_userid`/`member_identity_id` mismatch in its WHERE clause, mirroring the platform sibling's existing pattern.
- **A71-P1-4**: added `admin-observability.repository.spec.ts` asserting tenant scoping and the new atomic guard conditions in the UPDATE's WHERE clause.
- **A71-P1-5**: `AdminOperationLogService.record()` now logs swallowed failures via `Logger.warn` instead of a silent empty catch; spec updated to assert the log call.
- **A71-P1-3**: added a platform-token-rejection test covering all `admin-config` entry points, and a new `company-video-feature.controller.spec.ts` (previously had zero coverage) asserting the tenant route rejects platform sessions.
- **P2-2**: added `req.body.fields.mobile/.phone/.email/.wechat_id` to the pino redact paths.
- **P2-3**: added `@Throttle({ ttl: 60_000, limit: 20 })` to both admin-status PATCH routes, matching sibling write endpoints.
- **P2-4**: `updatePlatformAdminStatus` now validates its response via `platformAdminSummarySchema.parse()`.
- **P2-5**: added a repository test seeding `department/company/website` in the pre-edit blob and asserting an admin partial-update (that never mentions those keys) preserves them.
- **P2-6/P2-7**: added a shared `#pageLoadError` banner (index.html/styles.css/app.js) that `loadCurrentPage()` now drives: a neutral "加载中…" state while fetching, and a persistent (non-auto-dismissing) error message on failure, in addition to the existing toast.
- **P2-9**: closed as part of A71-P1-1's migration (`admin_operation_logs_created_idx`).
- **P2-10**: removed the two unused type aliases from `contracts/admin-observability.ts`.
- **Deferred**: A71-P2-1 (server-side PII masking policy for the `auditor` role) and A71-P2-8 (escapeLike/iso helper deduplication) — both require a product/architecture judgment call out of scope for this fix pass. Moved to `99_9999_deferred.md`.

Verification commands run after all fixes: `npm run typecheck` (clean), `npm run lint` (clean, 1 issue found and fixed — a floating promise in the new controller spec), `npm run build` (clean), `npm test -- --coverage --coverageThreshold='{}'` (58/58 suites, 295/295 tests passed, up from the pre-fix baseline of 56/288), `node database/scripts/validate-rls.cjs` (now passes; was failing before this fix pass).

## Doc Updates Needed
- `docs/99_audits/99_70_admin_design_gap_evaluation.md` §"鉴权现状与本轮方案" and the RLS/no-RLS rationale should be corrected: RLS is this project's default for `tenant_id`-bearing tables (20 tables in `database/rls.sql`), not something absent project-wide — the doc's framing that omitting RLS "matches project norms" is inaccurate and should be revised alongside whatever fix is chosen for A71-P1-1.
- `docs/01-specs/01_08_Admin_Backoffice_Architecture_Guide.md` describes a directory layout (`admin-platform/`/`admin-tenant/`) and route prefix (`/api/v1/admin/tenant/*`) that don't match the actual flat module layout or real routes (`/admin/operation-logs`, `/admin/platform/operation-logs`). Pre-existing staleness, not introduced by this range, but flagged since this audit touched the same surface — worth a follow-up sync.

## Evidence Log
- **Accepted A71-P1-1**: reproduced by direct execution (`node database/scripts/validate-rls.cjs` → threw, exit 1); cross-checked `database/rls.sql` (20 `ENABLE ROW LEVEL SECURITY` statements exist) against `database/migrations/migrate_v1_12.sql` (no RLS/FK) and `validate-rls.cjs:10-17` (table not in exceptions list).
- **Accepted A71-P1-2**: read `admin-auth.guard.ts:31-37` and `main.ts:23` directly; confirmed `trustProxy: "loopback"` is bypassed by the manual header parse; read the test asserting the spoofable value as expected.
- **Overturned during verification**: one review pass claimed "no RLS exists anywhere in this project," used to reject the missing-RLS finding as a non-issue. Direct grep found 20 RLS statements in `database/rls.sql` — the claim was based on checking only `schema.sql` (which never contains RLS statements; those live in the separate `rls.sql` file) and was factually wrong. Corrected in A71-P1-1 above.
- All other findings independently spot-checked by reading the cited file/line before inclusion; two agents (Health, Test & Ops) converged independently on the same root cause (missing RLS/FK) from different angles (coding-standard drift vs. CI-gate breakage), which corroborates the finding.

## 12-Dimension Coverage
1. Architecture: covered — module boundaries clean, no over-engineering found, 2 P2 duplication/index items.
2. Platform integration: N/A this range — no new external SDK/callback surface touched (WeCom sync untouched).
3. Security: covered — auth/authz isolation verified strong; IP-spoofing (P1) and redact/masking gaps (P2) found.
4. Code efficiency: covered — 2 P2 items (duplication, index).
5. Runtime smoothness: covered — no perf regressions found; index gap noted above.
6. Info isolation: covered — tenant/platform boundary strong; operation-log table is the exception (P1).
7. Data accuracy: covered — merge-fields fix verified correct; TOCTOU on admin-status guard (P1).
8. Parameter passing: covered — all new endpoints zod-validated, no unvalidated passthrough found.
9. UX: covered — masking/confirmation/guarding solid; error/loading state gaps (P2).
10. Coding standards: covered — clean overall; dead types and one skipped response-validation (P2).
11. Testing: covered (Deep) — full suite green, but real integration/e2e and IDOR-regression gaps found (P1 x2).
12. Deploy & Ops: covered (Deep) — CI gate genuinely enforces quality checks, but is currently broken (P1); audit-log failures are unobserved (P1); secrets/env hygiene otherwise solid.

## Next Steps
1. Fix A71-P1-1 (unblock CI deploy gate) — highest priority, affects every future backend deploy.
2. Fix A71-P1-2 (IP spoofing) — cheap, one-line change, protects audit-log integrity.
3. Fix A71-P1-6 (TOCTOU) and A71-P1-5 (silent log failures) — both small, high-value hardening.
4. Backfill A71-P1-3 / A71-P1-4 test gaps on privilege-affecting endpoints.
5. Batch P2 items during routine cleanup.
